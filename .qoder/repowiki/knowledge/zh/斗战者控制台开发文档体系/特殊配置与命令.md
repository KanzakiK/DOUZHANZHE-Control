- **管理员权限**：C# HAL 必须以管理员身份运行以加载内核驱动。
- **构建部署**：使用 `server/api/run.ps1` 自动执行前端 `npm run build` 并将产物嵌入 `wwwroot/`，随后启动 API 服务。
- **调试入口**：访问 `http://127.0.0.1:3100/debug` 可进行硬件寄存器读写测试及 WebSocket 遥测可视化。