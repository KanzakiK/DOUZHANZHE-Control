- 所有硬件控制端点采用统一的 POST /api/control 或特定功能路径（如 /api/smu/set），请求体使用 record 类型定义强类型模型。
- HAL 层对敏感硬件操作（如 EC 读写、物理内存映射）实施严格的异常捕获与降级策略，确保驱动不可用时返回安全默认值而非崩溃。
- 系统遥测数据（CPU/GPU温度、占用率）在 HAL 内部实现基于时间戳的缓存机制（通常 2-5 秒），避免高频触发外部进程或硬件查询。
- 配置文件（如 ui-state.json, custom-params.json）的写入采用“临时文件+原子移动”策略，防止并发写入导致的数据损坏。