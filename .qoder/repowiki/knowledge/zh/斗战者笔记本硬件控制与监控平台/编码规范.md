- 所有文档文件顶部必须包含带更新规则的 blockquote，明确架构变更、新增服务或废弃项的同步要求。
- 会话归档采用标题去重机制，新条目插入速查表分隔线前，且速查表仅保留最近 15 条记录以控制体积。
- 文件写入后自动执行 `_verify_write.py` 进行后验清洗，归一化行尾符、压缩多余空行并截断工具伪影。
- 前端状态管理采用每模式独立 localStorage 记忆，切换模式时自动保存旧值并加载新模式的历史参数。