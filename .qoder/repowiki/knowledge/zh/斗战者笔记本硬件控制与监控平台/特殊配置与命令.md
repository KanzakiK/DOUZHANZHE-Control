- **驱动自动加载**：`run.ps1` 启动时通过 `Start-Process -Verb RunAs` 提权安装并启动 `WinRing0` 驱动；`Program.cs` 启动时检测驱动状态并自动修复。
- **前端部署**：执行 `npm run build` 后由 `reload-fe.ps1` 将产物同步至 `server/api/bin/run/wwwroot/`，由 C# 服务直接托管。