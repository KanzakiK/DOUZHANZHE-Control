- 所有硬件控制指令均通过 `/api/control` 或专用端点（如 `/api/smu/set`）以 POST JSON 形式下发，并遵循统一的 `{ ok, error }` 响应结构。
- 前端状态管理与后端持久化（JSON 文件）通过去抖动的 API 调用保持同步，确保配置在重启后不丢失。
- 遥测数据通过单一 WebSocket 通道以 250ms 间隔推送全量快照，前端负责本地缓存与差值渲染以降低开销。