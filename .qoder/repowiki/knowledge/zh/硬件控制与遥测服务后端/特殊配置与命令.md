- **驱动加载**：启动时自动检测并尝试通过 `sc.exe` 安装/启动 `WinRing0_1_2_0` 内核驱动以支持 SMU 访问。
- **构建后钩子**：`.csproj` 中配置了 `AutoCommitAfterBuild` 目标，构建成功后自动执行 `git add -A` 和 `git commit`。
- **前端集成**：API 项目直接托管编译后的前端静态资源 (`wwwroot`)，并通过 `MapFallbackToFile` 支持 SPA 路由。