- 采用 CSS 变量（如 `var(--card)`, `var(--primary-2)`）结合 Tailwind 实用类实现多主题皮肤切换与样式隔离。
- 自定义 Hooks（如 `useControlState`, `useCardOrder`）集中处理复杂业务逻辑、副作用管理及本地存储同步。
- 服务层适配器（`uxtuAdapter.js`）统一导出常量映射（如 `MODE_PRESETS`, `thermalModeMap`）与异步 API 方法，实现前后端协议解耦。